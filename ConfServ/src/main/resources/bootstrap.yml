# HTTP Server
server:
  port: 8888

spring: 
  application:
    name: ConfigServ
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jvargad01/s-config-repository
          clone-on-start: true          